# TODO - UI 信息架构与交互原型梳理

版本：v0.9
日期：2026-07-09
状态：已完成

## 上一轮（v0.8）回顾

v0.8 已经把首页精简为「精神 + 活动列表」，活动卡片只留一个「进入活动」按钮。但实测后发现**信息密度问题没解决**：

1. **首页活动列表整段堆在 Hero 下方**，访客一进来既要看精神、又要看 SignalStrip、又要看 3 场活动卡片，注意力被切成三段。
2. **活动详情页直接把报名表单怼在脸上**：Hero + 状态轨道 + 赛道 + **报名表单** + 提交要求全部挤在一个页面，分不清「现在在浏览」还是「现在在报名」。
3. **缺少独立的报名编辑页**：报名动作和活动说明没有页面层级隔离，导致「加入比赛」这个关键决策点被弱化。
4. **缺少「我的报名」入口**：用户报名后没办法回看自己的状态，闭环不完整。

## 本轮目标

参考 aihacktour.com 的「静态展示 → 决策点 → 动作页」三级漏斗，把页面层级彻底拆开：

```
落地页（静态品牌）→ 活动列表 → 活动详情（决策）→ 报名编辑页（动作）→ 我的报名（闭环）
```

**每页只干一件事，按钮意图单一。**

## 目标页面层级

```
/                        落地页（纯静态品牌 + 精选活动）
/events                  活动列表（所有比赛一览 + 状态筛选）
/event/:id               活动详情（Hero + 状态轨道 + 赛道 + 提交要求）
/event/:id/join          报名编辑页（独立全屏表单，登录后才能进）
/my/registrations        我的报名（查看状态）
/projects                作品展示
/login                   登录（支持 ?redirect= 回跳）
/register                注册
/root                    后台管理
```

## 交互关键节点

```
访客流程：
  落地页 ──看精神──→ 「查看全部活动」
                     ↓
              活动列表 ──挑一个──→ 活动详情
                                     ↓
                                【加入比赛】（决策点）
                                     ↓
                                已登录？──否──→ /login?redirect=/event/:id/join
                                  │是
                                  ↓
                                报名编辑页 ──填表──→ 提交成功
                                                      ↓
                                                 /my/registrations（看状态）
```

## 执行清单

| # | 步骤 | 状态 | 说明 |
| --- | --- | --- | --- |
| 1 | 升级 TODO 文档到 v0.9 | 已完成 | 本文档。 |
| 2 | 重构路由表 | 已完成 | `router/index.js` 新增 `/event/:id/join`、`/my/registrations`，加 `requiresAuth` 路由守卫。 |
| 3 | 改造 AppHeader 导航 | 已完成 | 导航改为路由跳转；登录后头像区域可点进「我的报名」。 |
| 4 | LoginView 支持 redirect 回跳 | 已完成 | 登录成功读取 `?redirect=` 参数回跳，默认回首页。 |
| 5 | 首页 HomeView 纯静态化 | 已完成 | 删完整列表，只留 Hero + SignalStrip + Featured（1 张）+ Spirit，主 CTA 跳 `/events`。 |
| 6 | EventsView 强化 | 已完成 | 完整列表页，加 4 段状态筛选 pill；卡片双 CTA「查看活动 / 加入比赛」。 |
| 7 | EventView 去掉报名表单 | 已完成 | 删除 Register section，Hero 主 CTA「加入比赛 →」，文末次级 CTA「准备就绪，加入比赛」。 |
| 8 | 新建 EventJoinView 报名编辑页 | 已完成 | 独立全屏表单：面包屑 + 未登录拦截（带 redirect）+ 左栏摘要 + 右栏表单；提交成功跳 `/my/registrations`。 |
| 9 | 新建 MyRegistrationsView | 已完成 | 登录后查看报名状态；接口未就绪时给友好占位卡片，不影响浏览。 |
| 10 | 启动 dev 校验全链路 | 已完成 | `vite build` 通过；浏览器走完整链路：首页 → 列表 → 详情 → 加入比赛（拦截）→ 登录 redirect → 路由守卫；0 console error。 |
| 11 | commit + push + 文档版本号 | 进行中 | 推送 + 标记完成。 |

## 验收口径

- 首页 `/` 不再出现完整的活动列表，只有品牌信息 + 1-2 个 Featured 精选；主 CTA 指向 `/events`。
- 活动详情页 `/event/:id` **不再有报名表单**；报名通过「加入比赛」按钮跳转到独立的 `/event/:id/join`。
- 报名编辑页 `/event/:id/join` 是独立路由，未登录访问会自动跳到登录页并带 `redirect` 参数；登录后自动回跳。
- 提交报名成功后跳到 `/my/registrations`，能看到自己的报名记录（或接口未就绪时的占位提示）。
- 导航栏不再有失效的锚点（`#events`、`#spirit`）；登录后多出「我的报名」入口。
- 整条链路浏览器走通：首页 → 活动列表 → 活动详情 → 加入比赛 → 登录 → 报名 → 我的报名，无 console error。

## 变更记录

| 版本 | 日期 | 变更 |
| --- | --- | --- |
| v0.9 | 2026-07-09 | 启动「首页纯静态化 + 报名独立路由」迭代，新增 `/event/:id/join`、`/my/registrations`，重构导航与登录回跳。 |
| v0.8 | 2026-07-08 | 减弱 reveal 动画；首页精简为精神 + 活动列表；后台报名问题编辑器重做。 |
